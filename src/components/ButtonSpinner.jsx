import React from "react";
import Spinner from "./Spinner";

const ButtonSpinner = () => {
  return (
    <div>
      <button>
        <Spinner />
      </button>
    </div>
  );
};

export default ButtonSpinner;
